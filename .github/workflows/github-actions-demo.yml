name: Check Passed Tests

on:
  pull_request:
    #    branches:
    #      - main
    types:
      - opened
      - labeled
      - unlabeled

jobs:
  fail-for-passed-tests:
    if: !contains(github.event.pull_request.labels.*.name, 'passed-all-tests')
    runs-on: ubuntu-latest
    steps:
      - name: Fail if PR didn't Passed all Tests
        run: |
          echo "This PR needs testing."
          exit 1